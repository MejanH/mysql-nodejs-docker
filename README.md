# Mysql setup in Docker with Nodejs
---
Steps:
- Created basic rest api with nodejs built-in `http` module
- then installed `mysql` and `nodemon` dependency
- created `Dockerfile` and `.dockerignore` file and tested the api
- 